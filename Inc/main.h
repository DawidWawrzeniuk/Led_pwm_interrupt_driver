/*
 * main.h
 *
 * Created on: Mar 16, 2026
 * Author: dawi2
 */

#ifndef MAIN_H_          // Sprawdzenie: jeśli makro MAIN_H_ NIE jest jeszcze zdefiniowane...
#define MAIN_H_          // ...to zdefiniuj je (zabezpieczenie przed wielokrotnym dołączeniem pliku).

#include <stdint.h>      // Dołączenie standardowej biblioteki dla typów o stałej szerokości (np. uint32_t).
#include "STM32F303xE.h" // Dołączenie bazy definicji rejestrów i peryferiów dla mikrokontrolera STM32F303xE.


#endif /* MAIN_H_ */     // Koniec bloku warunkowego #ifndef.
